const { request } = require('@octokit/graphql/node_modules/@octokit/request')
const getUserAgent = require('@octokit/graphql/node_modules/universal-user-agent')

const version = require('@octokit/graphql/package.json').version
const userAgent = `octokit-graphql.js/${version} ${getUserAgent()}`

const withDefaults = require('@octokit/graphql/lib/with-defaults')

module.exports = withDefaults(request, {
  method: 'POST',
  url: '/graphql',
  headers: {
    'user-agent': userAgent
  }
})
